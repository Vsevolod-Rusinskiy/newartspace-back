## 1. Contract and ranking

- [x] 1.1 Add a public route with validated `limit` in the range 1..20.
- [x] 1.2 Implement normalized primary/additional theme and color extraction.
- [x] 1.3 Implement deterministic seven-group ranking with same-art-style preference and fallback.
- [x] 1.4 Exclude the source, hidden candidates, and duplicates.

## 2. Verification

- [x] 2.1 Add pure ranking tests for group order, normalization, fallback, tie-breaks, limit, and deduplication.
- [x] 2.2 Add service/controller contract coverage for public source visibility and query options.
- [x] 2.3 Run backend tests, build, strict OpenSpec validation, and local API smoke tests.
